---
- hosts: all
  become: yes
  tasks:
   - name: install webserver on ansible ubuntu node
     apt:
      name: apache2
      state: present

   - name: start webserver on ansible ubuntu node
     service:
       name: apache2
       state: started